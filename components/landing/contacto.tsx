const Contacto = ({id}:{id?:string}) => {
    return ( 
        <section id={id}>
            <p className="text-center font-semibold text-3xl mb-20 mt-32">Contacto</p>
        </section>
     );
}
 
export default Contacto;