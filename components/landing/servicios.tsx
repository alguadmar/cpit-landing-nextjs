const Servicios = ({id}:{id:string}) => {
    return ( 
        <section id={id}>
            <p className="text-center font-semibold text-3xl">Servicios</p>
            
        </section>
     );
}
 
export default Servicios;